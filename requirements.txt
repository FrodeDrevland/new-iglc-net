Django>=5.2,<5.3
wagtail>=7.0,<8.0
psycopg[binary]>=3.2
dj-database-url>=2.2
whitenoise>=6.7
gunicorn>=23.0
# Uploaded images and documents in Azure blob storage (production)
django-storages[azure]>=1.14
# Reading and writing PDFs (tracks from footers, headers and footers on proceedings)
pypdf>=6.0
reportlab>=4.2
# Word templates for the full proceedings
python-docx>=1.1
# The site documentation (docs/*.md) in the back office, under Help
markdown-it-py>=3.0
